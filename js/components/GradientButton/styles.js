import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {width: 200, alignSelf: 'center', paddingTop: 20},
  bar: {height: 40, justifyContent: 'center', borderRadius: 50},
  title: {
    color: 'white',
    alignSelf: 'center',
    fontWeight: '500',
    fontSize: 16,
  },
});

export default styles;
